#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_install:
	# Install the script to /usr/bin and remove the .sh extension
	install -Dm755 ../scripts/ntfscomp.sh $(CURDIR)/debian/ntfscomp/usr/bin/ntfscomp